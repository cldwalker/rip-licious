#!/usr/bin/env ruby
## Usage: rip-test PACKAGE [RAKE_OPTIONS]
##
## Runs a package's tests with `rake test`

require 'rip/script'

if dir = rip(:'find-package', ARGV.shift)
  Dir.chdir dir
  exec 'rake', *(ARGV << 'test')
end
