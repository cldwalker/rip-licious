#!/usr/bin/env ruby
# Usage: rip-git [*GIT_ARGUMENTS] PACKAGE
#
# Runs git commands from within a package's directory

require 'rip/script'

unless (dir = rip(:'find-package', ARGV.pop)).empty?
  Dir.chdir dir
  exec 'git', *ARGV
end
